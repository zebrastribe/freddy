# Multi-Object Architecture

Canonical architecture document for the object domain cutover.

This file supersedes the older pet-domain architecture naming and should be
used as the primary reference for:
- `users/{userId}/objects/{objectId}`
- `users/{userId}/objects/{objectId}/checkins/{checkinId}`
- `object_status/{objectId}`
- notification payloads using `objectId` and `objectName`
